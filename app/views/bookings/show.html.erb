<h1>Booking Details</h1>

<h2>Flight Information</h2>
<p>
  <strong>Departure Airport:</strong> <%= @booking.flight.departure_airport.name %><br>
  <strong>Arrival Airport:</strong> <%= @booking.flight.arrival_airport.name %><br>
  <strong>Departure Time:</strong> <%= @booking.flight.departure_time.strftime("%B %d, %Y %I:%M %p") %><br>
  <strong>Duration:</strong> <%= @booking.flight.duration_minutes %> minutes
</p>

<h2>Passengers</h2>
<ul>
  <% @booking.passengers.each do |passenger| %>
    <li>
      <strong>Name:</strong> <%= passenger.name %><br>
      <strong>Email:</strong> <%= passenger.email %>
    </li>
  <% end %>
</ul>

<%= link_to "Back to Search", root_path %>